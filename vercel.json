{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "ignorePackageJsonScripts": true,
        "buildCommand": "next build",
        "devCommand": "next dev",
        "outputDirectory": ".next"
      }
    }
  ]
}